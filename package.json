{
  "name": "succession-planning",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm install && npx prisma generate && npx prisma migrate deploy && node prisma/seed.js && node src/index.js",
    "build": "cd server && npm install && npx prisma generate",
    "install-server": "cd server && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
